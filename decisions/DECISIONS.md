# Decision Register

Log every decision worth remembering here: **date**, **status**, **reason**.

Before proposing or researching an approach, check this register first —
don't re-suggest something already tried and rejected without saying so.
It's fine to resurface a rejected approach if the prior rejection may have
been wrong or there's no viable alternative, but say explicitly that it
was tried before and why it's worth revisiting.

| Date | Decision | Status | Reason |
|------|----------|--------|--------|
| 2026-07-30 | Ported 3 conventions from a downstream consumer of this template: `Working.md` weekly-archive splitting, a generic destructive-command `ask` list in `.claude/settings.json`, and an opt-in `handoffs/` orchestrator/executor dispatch system | accepted | The downstream project hit real scaling pain this template didn't yet handle (a 1400+-line `Working.md`, no permission-level guard on destructive git commands, no pattern for parallelizable independent work streams); generalized the fixes back into the template so future projects start with them. `handoffs/` is opt-in scaffolding, not part of default BOOT setup. |
| 2026-08-18 | Ported a second, larger round of learnings from the same downstream project's continued development: a `## Parallel work (worktrees + stream registry)` section (opt-in, adopt alongside `handoffs/`), a sharpened dual-condition `Working.md` archive-cadence rule, a decisions entry-length trim rule, a `researcher` agent (paired with the existing `executor`), and commit-discipline / no-background-wait rules added to `executor.md` | accepted | The downstream project kept accumulating real process lessons (a cross-commit incident from an unscoped `git add -A`, executors losing uncommitted work to session limits, a `verify-the-verification` habit after a check that couldn't have caught the failure it was meant to catch) faster than they were being folded back here. All identifying specifics (project name, tenant/business details, real incident IDs) were deliberately generalized out — this repo is public and must not carry anything that identifies the downstream project or its data. |
| 2026-08-18 | Added `integrations/telegram-relay/` — an opt-in module (Telegram notifications + dual-channel local-popup/Telegram permission approval for Claude Code sessions) — and set the whole repo's license to MIT | accepted | Built after comparing three existing public projects that solve an adjacent problem (remote/Telegram control of Claude Code): `RichardAtCT/claude-code-telegram`, `JessyTsui/Claude-Code-Remote`, and `kcisoul/remotecode`. This module's approach differs architecturally (it answers a live hook's structured `PermissionRequest` rather than screen-scraping a terminal), but the comparison is credited in the module's `README.md` since it directly informed the design and named a real gap (injecting a fresh instruction into an idle session) scoped as a follow-up handoff rather than built blind. MIT chosen specifically for this repo (a generic, shareable template) — the downstream production project this content originated from stays under its own, different license/visibility. |
